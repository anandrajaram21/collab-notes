import type { NextConfig } from "next";

// Currently not using any custom Next.js configurations.
// If needed, add configurations here.
const nextConfig: NextConfig = {};

export default nextConfig;
